# Лабораторные работы по C#

## Запуск лабораторных работ

```bash
# Сборка всего решения
dotnet build SharpLabs.sln

# Запуск отдельных лабораторных
dotnet run --project Lab1/Lab1/Lab1.csproj
dotnet run --project Lab2/Lab2/Lab2.csproj
dotnet run --project Lab3/Lab3/Lab3.csproj
dotnet run --project Lab4/Lab4/Lab4.csproj
dotnet run --project Lab5/Lab5/Lab5.csproj
```

---

## Лабораторная работа 1: Использование среды разработки

### Задание 1.1: Форматирование числовой последовательности
Написать функцию, формирующую и возвращающую строку вида "1, 2, 3, ... N" (N передается как аргумент).

**Пример:**
- Вход: N = 7
- Выход: "1, 2, 3, 4, 5, 6, 7"

### Задание 1.2: Вывод квадрата из звездочек
Написать функцию, выводящую на экран квадрат из звездочек со стороной N. Центральная звездочка должна отсутствовать (для нечетных N ≥ 3).

**Пример для N=7:**
```
*******
*** ***
** * **
** * **
** * **
*** ***
*******
```

### Задание 1.3: Корпоративный CodeStyle
- Изучить регламент по C#
- Привести свой исходник к стандартам документа
- Особое внимание комментариям к классам, всем публичным методам и свойствам

---

## Лабораторная работа 2: Типы данных в .NET

### Задание 2.1: Калькулятор индекса массы тела (BMI)
Написать функцию для расчета индекса массы тела. Функция должна быть реализована в рамках консольной утилиты. Массу тела и рост необходимо запрашивать у пользователя, результат выводить на консоль.

**Формула:** BMI = вес (кг) / рост² (м)

### Задание 2.2: Обработка массива
Написать функцию, которая:
- Генерирует случайным образом элементы массива (число элементов и их тип определяются разработчиком)
- Определяет максимальное и минимальное значения
- Сортирует массив
- Выводит полученный результат на экран

**Важно:** LINQ запросы и готовые функции языка (Sort, Max и т.д.) использовать в данном задании запрещается.

### Задание 2.3: Анализ текста
Написать программу, которая определяет среднюю длину слова во введённой текстовой строке. Учесть, что символы пунктуации на длину слов влиять не должны. Регулярные выражения не использовать. Использовать стандартные методы классов string и char.

### Задание 2.4: Демонстрация типов данных
Добавить в программу:
- struct
- Добавить в качестве полей и свойств объектов разные типы данных, разобраться к какому типу памяти они относятся
- Передать разные объекты в параметре метода, посмотреть в чем разница при изменении объекта внутри метода
- Освоить ref, out параметры, понять в чем разница
- Попробовать принудительную сборку мусора

---

## Лабораторная работа 3: Коллекции, сравнение объектов

### Задание 3.1: Подсчет частоты слов
Написать программу подсчета частоты слов. Задан английский текст. Выделить отдельные слова и для каждого посчитать частоту встречаемости. Слова, отличающиеся регистром, считать одинаковыми. В качестве разделителей считать пробел и точку.

### Задание 3.2: Динамический массив
Написать программу для динамического массива:

**Требования к классу DynamicArray<T>:**
- На базе обычного массива (коллекции .NET не использовать) реализовать свой собственный класс `DynamicArray<T>`, представляющий собой динамический массив (массив с запасом) для хранения объектов произвольных типов
- Конструктор без параметров (создается массив емкостью 8 элементов)
- Конструктор с одним целочисленным параметром (создается массив с указанной емкостью)
- Конструктор, который в качестве параметра принимает коллекцию, реализующую интерфейс `IEnumerable<T>`, создает массив нужного размера и копирует в него все элементы из коллекции
- Метод `Add`, добавляющий в конец массива один элемент. При нехватке места для добавления элемента, емкость массива должна удваиваться
- Метод `AddRange`, добавляющий в конец массива содержимое коллекции, реализующей интерфейс `IEnumerable<T>`. Обратите внимание, метод должен корректно учитывать число элементов в коллекции с тем, чтобы при необходимости расширения массива делать это только один раз вне зависимости от числа элементов в добавляемой коллекции
- Метод `Remove`, удаляющий из коллекции указанный элемент. Метод должен возвращать true, если удаление прошло успешно и false в противном случае. При удалении элементов реальная емкость массива не должна уменьшаться
- Метод `Insert`, позволяющий добавить элемент в произвольную позицию массива (обратите внимание, может потребоваться расширить массив). Метод должен возвращать true, если добавление прошло успешно и false в противном случае. При выходе за границу массива должно генерироваться исключение `ArgumentOutOfRangeException`
- Свойство `Length` — получение количества элементов. Не путать с емкостью (Capacity)
- Свойство `Capacity` — получение емкости: длины внутреннего массива
- Методы, реализующие интерфейсы `IEnumerable` и `IEnumerable<T>`
- Индексатор, позволяющий работать с элементом по указанному номеру. При выходе за границу массива должно генерироваться исключение `ArgumentOutOfRangeException`

### Задание 3.3: Сравнение объектов
Попробовать в коде несколько видов коллекций (список, словарь, очередь, стек), разобраться как работать с коллекциями из кода.

### Задание 3.4: Реализация сравнения
Реализовать сравнение экземпляров своего класса с другим экземпляром этого же класса и с экземпляром совсем другого класса.

### Задание 3.5: Изучение yield
Самостоятельно изучить yield.

---

## Лабораторная работа 4: Делегаты и события

### Задание 4.1: Сортировка через делегат
Написать метод, реализующий упорядочивание массива произвольного типа. Принцип сравнения двух элементов должен передаваться в метод через делегат. Стандартные инструменты типа `Array.Sort` и `IComparable` не использовать.

### Задание 4.2: Класс с событиями
Завернуть метод в класс и реализовать в нём событие, сигнализирующее о завершении сортировки.

### Задание 4.3: События с аргументами
Добавить в свои классы событие[я] с передачей аргументов и без.

### Задание 4.4: Делегаты
Добавить делегат и попробовать с ним работать.

### Задание 4.5: Лямбда-выражения
Изучить лямбда-выражения и попробовать их на своем тестовом проекте.

---

## Лабораторная работа 5: Работа с XML, JSON и сериализация

### Задание 5.1: Режим сохранения/загрузки
Добавить режим работы консольной программы, при котором она сохраняет объект или коллекцию объектов в JSON, при котором производится десериализация из файла. Сериализация должна быть в автоматическом режиме.

### Задание 5.2: App.config (опционально)
Путь к файлу должен быть задан в App.Config, научиться работать с файлом конфигурации (читать и писать параметры).

---

## Требования ко всем лабораторным

### Корпоративный C# CodeStyle
- **PascalCase** для публичных членов (методы, свойства, классы)
- **_camelCase** для приватных полей (с подчеркиванием)
- **XML-комментарии** на русском языке для всех публичных методов и свойств
- **Валидация аргументов** для всех публичных методов
- **Использование var** где возможно
- **Всегда фигурные скобки** для if, for, while
- **Отсутствие magic numbers** - использование именованных констант

### Структура решения
Необходимо создать решение, состоящее из двух проектов:
- Библиотека с классами
- Консольная утилита для демонстрации работы

---

## Технологии
- .NET 9
- C# 13
- System.Text.Json для сериализации
- System.Configuration.ConfigurationManager для конфигурации
